# certsubs

### Usage
```
Usage of ./certsubs:
  -p string
        Ports to connect, separate with comma. (Default: 8080,443) (default "443")
  -t int
        Threads to use. (Default: 5) (default 5)
```